

const Profile = () => {

    // LOGIN
  
    /* const { setUserInfo } = useContext(UserContext); 
    
        const handleLogin = () => {
            setUserInfo({});
        }

    */
   

    return (<div>Profile </div> )
}

export default Profile